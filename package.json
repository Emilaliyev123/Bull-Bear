{
  "name": "bull-bear-arbitrage-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Bull & Bear Arbitrage Scanner SaaS with academy content, subscriptions, admin uploads, and live exchange scanning.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "test": "node --env-file=.env --test"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
